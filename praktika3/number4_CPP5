#include <iostream>
#include <cstdlib>

int main() {
    int n = 7;
    int m = 8;
    int arr[n][m];
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            arr[i][j] = rand()%10;
            std::cout << arr[i][j] << " ";
        }
    }
}
